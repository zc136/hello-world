C语言：一种结构化的程序性设计的语言。
主要包括三种结构;顺序结构，选择结构，循环结构。
分支语句：if swith
循环语句：while for （do while）
语句：由逗号隔开的为一个语句。
练习1，输入1-100之间的所有奇数。
#include

int main()
{
int a= 0;
while(a<=100)
{
 if(a%2==1)
 printf("a=%d\n",a);
 a++;
}
return 0;
}
